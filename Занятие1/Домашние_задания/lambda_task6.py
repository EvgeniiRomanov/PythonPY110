# Задание 6.
# Напишите скрипт, чтобы найти максимальное значение в заданном неоднородном списке с помощью лямбда.
# Пример списка: ['Python', 3, 2, 4, 5, 'version']

def max_elem(list_):

    if list_
    print(list_, type(list_))
    #list_ = [str(word) for word in list_]
    #list_
    return

if __name__ == "__main__":

    list_ = [3, 'a', 'tre', 2, 10, 4, 12]
    k = map(max_elem, list_)
    print(list(k))
    #list_1 = map(lambda x: str(x), list_)
    # print(list_1)
    # k1 = list(filter(str.isdigit, list_1))
    # #k1 = list(filter(str.isdigit, list_1))
    # print(k1)
    # print(max(k1, key=)
    # #chars = filter(str.isdigit, str_)



    #
    # def task(words: list) -> list:
    #     # return [word.upper() for word in words]  # TODO перевести слова в верхний регистр c помощью map
    #     return list(map(str.upper, words))
    #
    #
    # if __name__ == "__main__":
    #     list_words = [
    #         "Goldenrod",
    #         "Purple",
    #         "Salmon",
    #         "Turquoise",
    #         "Cyan"
    #     ]